
import 'package:flutter/material.dart';

class UnsupportedView extends StatelessWidget {
  const UnsupportedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming soon ...'),
    );
  }
}
